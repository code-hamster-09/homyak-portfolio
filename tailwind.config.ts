import type { Config } from "tailwindcss";

const config: Config = {
  // Контент, который Tailwind будет сканировать для генерации классов
  content: [
    "./src/pages/**/*.{js,ts,jsx,tsx}",
    "./src/components/**/*.{js,ts,jsx,tsx}",
    "./src/app/**/*.{js,ts,jsx,tsx}",
  ],

  // Настройка темы
  theme: {
    // В секции 'extend' мы добавляем наши кастомные настройки,
    // сохраняя при этом стандартную палитру Tailwind.
    extend: {
      // Кастомные цвета
      colors: {
        // Фоновые и основные цвета
        "bg-main": "#0E0E10", // Фон: почти чёрный, с лёгким синим оттенком
        "card-bg-layer": "rgba(255, 255, 255, 0.05)", // Для использования с произвольными значениями (bg-[rgba(255,255,255,0.05)]) или backdrop-filter

        // Акцентные цвета
        "accent-purple": "#8B5CF6", // Чистый, мягкий фиолетовый
        "accent-blue": "#3B82F6", // Холодный синий
        "accent-secondary": "#6366F1", // Между синим и фиолетовым

        // Текстовые цвета
        "text-primary": "#E2E8F0", // Светло-серый (основной текст)
        "text-secondary": "#94A3B8", // Приглушённый серый (вторичный текст)

        // Цвета состояний
        "status-success": "#10B981", // Изумрудный
        "status-error": "#EF4444", // Мягкий красный
      },

      // Дополнительные настройки (например, для градиентов или теней),
      // которые вы можете захотеть добавить позже.
      // Например:
      // backgroundImage: {
      //   'hero-gradient': 'linear-gradient(to right, #8B5CF6, #3B82F6)',
      // },
    },
  },

  // Добавление плагинов, если они есть (у вас они пока не указаны, кроме 'tw-animate-css')
  // Если вы используете плагины, их нужно добавить сюда.
  plugins: [
    // require('tw-animate-css'),
    // Если вам нужно использовать плагин, убедитесь, что он правильно подключен
  ],
};

export default config;
